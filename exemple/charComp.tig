let
    function isInBonjour (c:string) = 
    if c == B then return(1) else
        if c == o then return(1) else
            if c == n then return(1) else
                if c == j then return(1) else
                    if c == u then return(1) else
                        if c == r then return(1) else break
in
    isInBonjour("g")
end